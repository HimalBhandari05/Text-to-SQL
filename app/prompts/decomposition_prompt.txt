You are a query analysis expert for a PostgreSQL database called ClassicModels.

Analyze the following natural language question and extract its structured components.

## Question
{question}

## Available Tables
customers, orders, orderdetails, products, productlines, employees, offices, payments

## Instructions
Return a JSON object with exactly these five fields:
- "intent": one clear sentence describing what the question is asking
- "tables": array of table names needed to answer the question
- "columns": array of specific column names needed
- "filters": SQL WHERE condition string (e.g. country = 'USA'), or "None"
- "joins": full SQL join condition (e.g. orders.customerNumber = customers.customerNumber), or "None"

Return ONLY valid JSON. No explanation. No markdown. No backticks.

## Example
Question: How many customers are from the USA?

{
  "intent": "Count the total number of customers where country is USA",
  "tables": ["customers"],
  "columns": ["customerNumber"],
  "filters": "country = 'USA'",
  "joins": "None"
}
